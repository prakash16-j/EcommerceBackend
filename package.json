{
  "name": "ecommerce-backend",
  "version": "1.0.0",
  "description": "Mock API backend for my Ecommerce project using JSON Server",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
